/**
 * MiniMarket Client
 * 
 * Note: Codama client generation is not available due to PDA seed 
 * referencing instruction arguments. Use the Anchor Program API directly
 * via the parent minimarket-exports module.
 * 
 * For usage, import from the root:
 * import { getMinimarketProgram } from '../../index'
 */

// Re-export types and utilities from parent
export * from '../../minimarket-exports'
